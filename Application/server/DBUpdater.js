/* DBUpdater.js: Javascript function that will take update the value in our DB for the given key.
Deliberately generic and unaware of the contents of the DB*/

export function updateDB(key, value) {
}